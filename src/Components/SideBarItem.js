import styles from './SideBarItem.module.css';

const SideBarItem = (props) => {
    const icon = '../Icons/' + props.title + '.png';

    return <div>
        <img alt="a" src="../Icons/Home.png"  title='abc'/>
    </div>
}

export default SideBarItem;